import { Routes } from '@angular/router';
import { QuotationComponent } from './quotationmagement/quotation.component';
export const routes: Routes = [{ path: 'quotationmagement', component: QuotationComponent }];
